#
# Author: Marie Rognes
# Date: 2008-10-03
#

RT = FiniteElement("Raviart-Thomas", "tetrahedron", 1)
DG = FiniteElement("DG", "tetrahedron", 0)
MX = RT + DG

(u, p) = TrialFunctions(MX)
(v, q) = TestFunctions(MX)

a0 = (dot(u, v) + div(u)*q + div(v)*p)*dx

a1 = (dot(u, v) + div(u)*q + div(v)*p)*dx - p*dot(v, n)*ds

